<!--index.wxml-->
<import src="/template/loading/index" />
<view class="container">
  <view wx:for="{{list}}" wx:key="index" class="w-100 p-tb-50 ta-center fs-40">{{item.name}}</view>
  <template is="loading" data="{{loading, more}}" />
</view>